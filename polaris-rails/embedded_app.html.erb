<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <% application_name = ShopifyApp.configuration.application_name %>
    <title><%= application_name %></title>
    <link rel="stylesheet" href="https://sdks.shopifycdn.com/polaris/1.2.1/polaris.min.css">
    <%= stylesheet_link_tag 'application' %>
    <%= csrf_meta_tags %>
    <% if Rails.env.development? %>
        <!-- connect react-devtools -->
        <script src="https://localhost:8097"></script>
    <% end %>
    <script>
      window.appConfig = {
        apiKey: "<%= j ShopifyApp.configuration.api_key %>",
        shopOrigin: "<%= "https://#{ @shop_session.url }" if @shop_session %>",
        debug: <%= Rails.env.development? ? 'true' : 'false' %>,
      }

      // force redirect to Shopify admin
      if (!window.parent) {
        window.location = appConfig.shopOrigin + '/admin/apps/' + appConfig.apiKey + window.location.pathname.replace('/embedded', '')
      }
    </script>
    <%= javascript_pack_tag 'application' %>
  </head>
  <body>
    <div class="app-wrapper">
      <div class="app-content">
        <main role="main" id="main"></main>
      </div>
    </div>
  </body>
</html>
